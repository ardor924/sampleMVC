/*스프링 자바스크립트 로드 에러로 JS파일에서의 직접적인 작업중단*/
/* 

document.addEventListener("DOMContentLoaded", function(){
 	if(${login_msg == 'success'}){
		alert("로그인 성공!");
		};
	if(${login_msg == 'fail'}){
		alert("로그인 실패!");
		};  
});

	
*/